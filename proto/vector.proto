syntax = "proto3";

import "tool.proto";

package vector;

service Vector {
  rpc QueryByContent(TextQuery) returns (tool.QueryResult);
  rpc QueryByDescriptor(TextQuery) returns (tool.QueryResult);
}

message TextQuery {
  string text = 1;
  optional tool.QueryFilter filter = 2;
  optional string github_token = 3;
}
