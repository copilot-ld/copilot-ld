# Changelog

## 2025-11-16

- Created `@copilot-ld/libanalysis` package with `formatForAnalysis()` helper
  function
- Extracted core markdown formatting logic from `scripts/code.js`
- Added support for custom formatting options including `printWidth`,
  `proseWrap`, and `padWidth`
