/* eslint-env node */

/**
 * Protocol Buffer code generation utilities
 *
 * This package provides tools for generating code from Protocol Buffer definitions.
 * The main functionality is available via the `codegen` binary.
 */

// Re-export any shared utilities if needed
export {};
