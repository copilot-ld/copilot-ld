# Changelog

## 2026-01-18

- Initial release with functions extracted from libutil and scripts
- Added `updateEnvFile()` for environment file management
- Added `generateHash()` for deterministic hashing
- Added `generateUUID()` for unique identifier generation
- Added `generateSecret()` for hex-encoded random secrets
- Added `generateBase64Secret()` for base64url encoded random secrets
- Added `generateJWT()` for HS256-signed JWT creation
