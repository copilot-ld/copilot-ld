# Changelog

## 2026-01-26

- Initial release with `<chat-drawer>`, `<chat-page>`, and `<chat-user>` web
  components for authentication-aware chat interfaces
- Composition-based architecture with `ChatClient`, `ChatApi`, `ChatAuth`, and
  `ChatState` classes for flexible integration
- Responsive design with dark mode support and ChatGPT-like styling
