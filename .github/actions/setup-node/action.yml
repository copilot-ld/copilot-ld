name: "Setup Node.js"
runs:
  using: "composite"
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "22"
        registry-url: "https://npm.pkg.github.com"
        cache: "npm"

    - name: Install dependencies
      shell: bash
      run: npm ci
