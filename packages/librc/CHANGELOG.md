# Changelog

## 2026-01-26

- Initial release with `ServiceManager` for svscan communication via Unix socket
- Supports `start`, `stop`, and `status` commands with optional service name for
  dependency-aware operations
- RFC 5424 logging format with `--silent` flag option
